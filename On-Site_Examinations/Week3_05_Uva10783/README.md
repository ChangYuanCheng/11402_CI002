# UVa 10783 - Odd Sum

## 題目說明
給定多筆測資，每筆包含兩個整數 `a` 和 `b`，請計算區間 `[a, b]` 中所有「奇數」的總和。

---

## 解題思路
1. 讀入測資筆數 T
2. 對每一筆：
   - 讀入 a, b
   - 若 a 是偶數，先讓 a + 1（從第一個奇數開始）
   - 從 a 開始，每次加 2（只走奇數）
   - 累加直到 b
3. 輸出結果

---

## 輸出格式
每筆測資需輸出：
