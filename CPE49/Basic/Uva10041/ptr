include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main（ ）｛
   int T；
   cin >> T;
   while(T--){
      int n ；
      cin >> n;
      
      vector < int > a ；
      for （int i = 0 ; i < n ; i + + ）｛
          cin >> a ［i ］；
      ｝
      sort （a. begin(）, a.end());
       int mid = a[n /2 ] ；
     
      
   }

｝